import Header from '@components/Header';
import styles from './Home.module.css';

const Home = () => {

    return (
        <div className={ styles.home }>
            <Header></Header>
        </div>
    );

}

export default Home;
