import styles from './Home.module.css';

const Home = () => {
    return (
        <div className={ styles['wrapper'] }>

                    </div>
                </div>
            </div>
        </div>
    );
};

export default Home;
