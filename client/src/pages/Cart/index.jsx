import styles from './Cart.module.css';

const Home = () => {
    return (
        <div className={ styles['wrapper'] }>
            <h1>This is the Cart page.</h1>
        </div>
    );
};

export default Cart;
